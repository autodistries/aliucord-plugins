version = "0.1.0"
description = "Fixes voice calls by implementing DAVE (Discord Audio/Video E2EE) protocol support"


aliucord.changelog.set("""
    # 1.0.6 Changed the /timestamp to /snowflake because conflicted with existing plugin
    # 1.0.4
    * Created a separate plugin for Whois
""".trimIndent()

)
